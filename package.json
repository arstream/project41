{
  "name": "project41",
  "version": "1.0.0",
  "description": "testing json-server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.12.0"
  }
}
